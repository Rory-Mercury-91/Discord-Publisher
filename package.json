{
  "name": "publication-generator-tauri",
  "version": "1.0.16",
  "description": "Application Tauri pour générer et publier des traductions sur Discord",
  "author": "Rory Mercury 91",
  "scripts": {
    "dev": "tauri dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "bump-version": "node scripts/bump-version.js",
    "set-version": "node scripts/set-version.js",
    "build:win": "npm run bump-version && tauri build",
    "build": "npm run bump-version && tauri build",
    "test": "npm --prefix frontend run type-check && npm --prefix frontend run build",
    "tauri": "tauri",
    "supabase:login": "supabase login",
    "supabase:link": "supabase link --project-ref ffsdgocbhghyermqqwlv",
    "supabase:deploy-fn": "supabase functions deploy validate-tags-master-code --no-verify-jwt",
    "supabase:db-push": "supabase db push"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "supabase": "^2.0.0"
  }
}
